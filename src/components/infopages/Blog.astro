<section class="py-16 bg-white flex justify-center items-center">
  <div class="w-full max-w-4xl px-4 text-left">
    <h2 class="text-4xl font-bold mb-8">Blog</h2>
    <!-- Responsive Grid Layout -->
    <div id="blog-container" class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      <!-- Blog Cards -->
      <div class="blog-card w-full mx-auto rounded-lg shadow-md overflow-hidden bg-white hover:shadow-lg transition duration-300">
        <img src="https://callsupport-old.netlify.app/_next/image?url=%2F2.png&w=750&q=75" alt="Blog Image 1" class="w-full h-48 object-cover">
        <div class="p-4">
          <h3 class="text-lg font-semibold mb-2">How AI Phone Agents Change Customer Support for All Ecommerce Businesses</h3>
          <p class="text-gray-600 text-sm">Learn how AI can revolutionize your customer support.</p>
        </div>
      </div>
      <!-- Blog Card 2 -->
      <div class="blog-card w-full mx-auto rounded-lg shadow-md overflow-hidden bg-white hover:shadow-lg transition duration-300">
        <img src="https://callsupport-old.netlify.app/_next/image?url=%2F3.png&w=750&q=75" alt="Blog Image 2" class="w-full h-48 object-cover">
        <div class="p-4">
          <h3 class="text-lg font-semibold mb-2">How to Improve and Optimize Voice AI Performance</h3>
          <p class="text-gray-600 text-sm">Tips to optimize the performance of voice AI systems.</p>
        </div>
      </div>
      <!-- Blog Card 3 -->
      <div class="blog-card w-full mx-auto rounded-lg shadow-md overflow-hidden bg-white hover:shadow-lg transition duration-300">
        <img src="https://callsupport-old.netlify.app/_next/image?url=%2F3.png&w=750&q=75" alt="Blog Image 3" class="w-full h-48 object-cover">
        <div class="p-4">
          <h3 class="text-lg font-semibold mb-2">How to Make Millions with Your E-commerce Store This Black Friday</h3>
          <p class="text-gray-600 text-sm">Strategies to maximize your e-commerce sales this season.</p>
        </div>
      </div>
      <!-- Blog Card 4 -->
      <div class="blog-card w-full mx-auto rounded-lg shadow-md overflow-hidden bg-white hover:shadow-lg transition duration-300">
        <img src="https://callsupport-old.netlify.app/_next/image?url=%2F2.png&w=750&q=75" alt="Blog Image 4" class="w-full h-48 object-cover">
        <div class="p-4">
          <h3 class="text-lg font-semibold mb-2">How AI Phone Agents Change Customer Support for All Ecommerce Businesses</h3>
          <p class="text-gray-600 text-sm">Understand the impact of AI in e-commerce customer service.</p>
        </div>
      </div>
      <!-- Blog Card 5 -->
      <div class="blog-card w-full mx-auto rounded-lg shadow-md overflow-hidden bg-white hover:shadow-lg transition duration-300">
        <img src="https://callsupport-old.netlify.app/_next/image?url=%2F3.png&w=750&q=75" alt="Blog Image 5" class="w-full h-48 object-cover">
        <div class="p-4">
          <h3 class="text-lg font-semibold mb-2">How to Leverage AI for Better E-commerce Sales</h3>
          <p class="text-gray-600 text-sm">Explore how AI tools can boost your sales strategy.</p>
        </div>
      </div>
    </div>

    <!-- Pagination Controls -->
    <div class="flex flex-col sm:flex-row items-center justify-between border-t border-gray-200 bg-white px-4 py-3 sm:px-6 mt-8">
      <!-- Mobile Pagination -->
      <div class="flex justify-between w-full sm:hidden">
        <button id="prev-btn" class="relative inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50">Previous</button>
        <button id="next-btn" class="relative ml-3 inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50">Next</button>
      </div>
      <!-- Desktop Pagination -->
      <div class="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
        <div>
          <p class="text-sm text-gray-700">
            Showing <span class="font-medium" id="start-count">1</span> to
            <span class="font-medium" id="end-count">4</span> of
            <span class="font-medium" id="total-count">5</span> results
          </p>
        </div>
        <div>
          <nav class="isolate inline-flex -space-x-px rounded-md shadow-sm" aria-label="Pagination">
            <button id="prev-btn-desktop" class="relative inline-flex items-center rounded-l-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
              <span class="sr-only">Previous</span>
              <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                <path fill-rule="evenodd" d="M11.78 5.22a.75.75 0 0 1 0 1.06L8.06 10l3.72 3.72a.75.75 0 1 1-1.06 1.06l-4.25-4.25a.75.75 0 0 1 0-1.06l4.25-4.25a.75.75 0 0 1 1.06 0Z" clip-rule="evenodd" />
              </svg>
            </button>
            <span id="pagination-numbers" class="inline-flex items-center"></span>
            <button id="next-btn-desktop" class="relative inline-flex items-center rounded-r-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
              <span class="sr-only">Next</span>
              <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                <path fill-rule="evenodd" d="M8.22 5.22a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 0 1-1.06-1.06L11.94 10 8.22 6.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" />
              </svg>
            </button>
          </nav>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const blogCards = document.querySelectorAll('.blog-card');
  const nextBtn = document.getElementById('next-btn');
  const prevBtn = document.getElementById('prev-btn');
  const nextBtnDesktop = document.getElementById('next-btn-desktop');
  const prevBtnDesktop = document.getElementById('prev-btn-desktop');
  const pageNumberContainer = document.getElementById('pagination-numbers');
  const startCountEl = document.getElementById('start-count');
  const endCountEl = document.getElementById('end-count');
  const totalCountEl = document.getElementById('total-count');

  let currentPage = 1;
  const blogsPerPage = 4;
  const totalBlogs = blogCards.length;
  const totalPages = Math.ceil(totalBlogs / blogsPerPage);

  // Function to show blogs for the current page
  function showBlogsForPage(page) {
    const start = (page - 1) * blogsPerPage;
    const end = Math.min(start + blogsPerPage, totalBlogs);

    blogCards.forEach((card, index) => {
      card.style.display = index >= start && index < end ? 'block' : 'none';
    });

    startCountEl.textContent = start + 1;
    endCountEl.textContent = end;
    totalCountEl.textContent = totalBlogs;

    // Update pagination buttons
    prevBtn.style.display = nextBtn.style.display = prevBtnDesktop.style.display = nextBtnDesktop.style.display = 'block';
    if (page === 1) prevBtn.style.display = prevBtnDesktop.style.display = 'none';
    if (page === totalPages) nextBtn.style.display = nextBtnDesktop.style.display = 'none';

    // Update page numbers
    pageNumberContainer.innerHTML = '';
    for (let i = 1; i <= totalPages; i++) {
      const pageButton = document.createElement('a');
      pageButton.href = '#';
      pageButton.className = `relative inline-flex items-center px-4 py-2 text-sm font-semibold ${i === page ? 'bg-indigo-600 text-white' : 'text-gray-900 ring-1 ring-inset ring-gray-300 hover:bg-gray-50'}`;
      pageButton.textContent = i;
      pageButton.addEventListener('click', (e) => {
        e.preventDefault();
        currentPage = i;
        showBlogsForPage(currentPage);
      });
      pageNumberContainer.appendChild(pageButton);
    }
  }

  // Event listeners for pagination
  nextBtn.addEventListener('click', () => {
    if (currentPage < totalPages) {
      currentPage++;
      showBlogsForPage(currentPage);
    }
  });

  prevBtn.addEventListener('click', () => {
    if (currentPage > 1) {
      currentPage--;
      showBlogsForPage(currentPage);
    }
  });

  nextBtnDesktop.addEventListener('click', () => {
    if (currentPage < totalPages) {
      currentPage++;
      showBlogsForPage(currentPage);
    }
  });

  prevBtnDesktop.addEventListener('click', () => {
    if (currentPage > 1) {
      currentPage--;
      showBlogsForPage(currentPage);
    }
  });

  // Initial state
  showBlogsForPage(currentPage);
</script>
